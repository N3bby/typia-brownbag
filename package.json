{
  "name": "typia-brownbag",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf dist && npx tsc && node dist/example.js",
    "start-db": "rm -rf dist && npx tsc && node dist/example-database.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "ts-patch install && typia patch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-patch": "^3.3.0",
    "typescript": "~5.7.3"
  },
  "dependencies": {
    "typia": "^7.6.3"
  }
}
